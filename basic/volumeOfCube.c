// volume of cube -> side * side * side 

#include <stdio.h>
void main() {
    int side;
    printf("Enter side : ");
    scanf("%d", &side);

    // int ans = side * side * side;
    // printf("ans = %d ", ans);
    // * or 
    printf("ans = %d ", side * side * side);
}