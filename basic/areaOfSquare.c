// side -> input -> area of square = side * side 
#include <stdio.h>
void main() {
    int side;
    printf("Enter side : ");
    scanf("%d", &side);

    // int ans = side * side;
    printf("ans =  %d", side * side);
}